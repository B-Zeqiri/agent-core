# Postgres (local dev)
PERSIST_DB_DRIVER=postgres
PG_URL=postgres://postgres:your_password@localhost:5432/agent_core

# Server
PORT=3000
WORKER_ONLY=

# Optional: queue config
QUEUE_DRIVER=local
QUEUE_START_WORKER=true
